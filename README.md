# CV app

A simple CV builder application written using React. It allows to build a basic
CV and export it as PDF or print it directly using the browser's native printing
functionality. The main goal of this project is to get a grasp of the basics of
React.
